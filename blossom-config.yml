# Blossom server configuration
# See https://github.com/hzrd149/blossom-server for full options

publicDomain: blossom.tubestr.app

storage:
  backend: local

upload:
  enabled: true
  requireAuth: true

media:
  enabled: true

dashboard:
  enabled: true
  password: ${BLOSSOM_ADMIN_PASSWORD}
